const name = require("./people")
console.log(name.people, name.ages) // the name will give an out of the "modul.export value given to it"


const os = require('os')
console.log(os.platform(), os.homedir())